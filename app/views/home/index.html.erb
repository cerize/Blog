
    <% categories = Category.all %>
    <% @categories.each do |category| %>
      <%= link_to category.title, post_category_path(category.title), class: "btn btn-primary btn-modification"  %>
   <% end %>
